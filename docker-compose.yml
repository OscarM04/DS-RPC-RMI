version: '3.0'
services:

  db_Storage:
    image: postgres:10-alpine
    ports:
      - '5432:5432'
    environment:
      - 'POSTGRES_USER=root'
      - 'POSTGRES_PASSWORD=root'
      - 'POSTGRES_DB=bank'
